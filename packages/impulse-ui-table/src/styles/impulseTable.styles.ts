import { ComponentStyleProps, ContainerStyleProps } from '@impulse-ui/types';

const impulseTableContainerProps: ComponentStyleProps<ContainerStyleProps> = {
  hasDropShadow: true,
  hasBorderRadius: true,
};

export { impulseTableContainerProps };
