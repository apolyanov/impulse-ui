import { Property } from 'csstype';

type BackgroundColor = Property.BackgroundColor;
type BorderColor = Property.BorderColor;
type Color = Property.Color;

export type { BackgroundColor, BorderColor, Color };
